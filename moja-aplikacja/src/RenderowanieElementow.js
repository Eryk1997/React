import React from 'react';
import ReactDOM from 'react-dom';

//Renderowanie elementow
const element = <h1>Witaj brzdąc</h1>

ReactDOM.render(
    element,
    document.getElementById('renderowanie')
);
serviceWorker.unregister();
